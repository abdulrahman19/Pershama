let items = new Set(['one', 'two', 'three', 'one', 'two']);

// console.log(items);

// console.log(items.size);

// items.add('foo');

// items.delete('two');

// console.log(items);

// console.log(items.has('foo'));

// items.forEach(item => console.log(item));

// // convert it to array to filter out
// let arr = [...items];

// console.log(arr.lenght);

items.clear();

console.log(items);
